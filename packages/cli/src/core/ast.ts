import { api } from '@cliz/cli';

export function parse(markdown: string) {
  // @TODO
}

// export function generate(data): string {

// }

// api.fs.readFile('/tmp/261.md', { encoding: 'utf-8' }).then(() => {
//   // @TODO
// });
